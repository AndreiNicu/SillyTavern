# NPC Memory Contract — moved

This contract is now a **shared, versioned document** kept in one place so the
World-Forge pipeline and this fork can't drift apart.

- **Canonical source of truth:** [`AndreiNicu/World-Forge`](https://github.com/AndreiNicu/World-Forge) → `contracts/MEMORY_CONTRACT.md`
- **Mirrored read-only into this fork at:** [`/contracts/MEMORY_CONTRACT.md`](../../../../contracts/MEMORY_CONTRACT.md)

Do not edit a copy. Edit the canonical file in World-Forge, then run
`scripts/sync-contracts.sh` here to refresh the mirror. A CI drift check keeps
the fork's copy byte-identical to canonical.

See also the companion [`WORLD_FORGE_SYNC.md`](../../../../contracts/WORLD_FORGE_SYNC.md),
which covers the runtime seams (Director-card tag, `</style_contract>` marker,
scene-name reconciliation) that this data contract doesn't.
